---
title: astro-better-admonitions
description: Extensible callout boxes and inline badges for Astro MDX.
order: 6
---

import { Admonition } from 'astro-better-admonitions';

# astro-better-admonitions

Callout boxes (note, tip, caution, warning, danger) and inline badge components for MDX.
All types are configurable, and new types can be added.

<p class="pkg-meta">Current version: <strong>0.1.1</strong> &mdash; <a href="https://www.npmjs.com/package/astro-better-admonitions" target="_blank" rel="noopener">npm</a></p>

## Installation

```shell-session
npm install astro-better-admonitions
```

Import the CSS in your global stylesheet:

```css
@import "astro-better-admonitions/style.css";
```

## Usage

```mdx
import { Admonition } from 'astro-better-admonitions';

<Admonition type="note" title="Something to know">
  This is a note callout.
</Admonition>

<Admonition type="tip" title="Pro tip">
  This is a tip callout.
</Admonition>

<Admonition type="caution" title="Be careful">
  This is a caution callout.
</Admonition>

<Admonition type="warning" title="Warning">
  This is a warning callout.
</Admonition>
```

## Live examples

<Admonition type="note" title="Note">
  Use notes for supplementary information that does not block the reader.
</Admonition>

<Admonition type="tip" title="Tip">
  Tips highlight helpful shortcuts or best practices.
</Admonition>

<Admonition type="caution" title="Caution">
  Caution boxes warn about actions that could cause problems.
</Admonition>

<Admonition type="warning" title="Warning">
  Warnings indicate something that will definitely cause issues if ignored.
</Admonition>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `type` | `string` | `note` | Callout type: `note`, `tip`, `caution`, `warning`, or a custom type |
| `title` | `string` | -- | Callout heading (defaults to the type label) |

## Custom types

Pass a `types` prop with additional type configs to add new callout types:

```astro
---
import { Admonition } from 'astro-better-admonitions';
const customTypes = {
  important: { label: 'Important', color: '#7c3aed' },
};
---
<Admonition type="important" title="Important" types={customTypes}>
  This is a custom callout.
</Admonition>
```

The `color` property sets the left border and icon color.
